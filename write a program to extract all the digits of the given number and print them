#include<stdio.h>
int main(){
    int i,num;
    printf("enter number");
    scanf("%d",&num);
    while(num !=0){
          int rem = num%10;
          printf("%d",rem);
          num = num/10;
    }
        return 0;
}                 
